# Generate a value with: openssl rand -hex 32
# Requirements: at least 16 visible ASCII characters and no spaces.
SYNC_SERVER_TOKEN=replace-with-a-random-64-character-hex-token

# Optional upload limits in bytes.
SYNC_SERVER_MAX_OBJECT_BYTES=4294967296
SYNC_SERVER_MAX_MANIFEST_BYTES=67108864
